# https://editorconfig.org — keeps shell scripts Linux-safe on Windows checkouts.
root = true

[*]
charset = utf-8
insert_final_newline = true
trim_trailing_whitespace = true

[*.sh]
end_of_line = lf

[*.sql]
end_of_line = lf

[*.md]
trim_trailing_whitespace = false
